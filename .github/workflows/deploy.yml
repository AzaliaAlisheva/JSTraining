name: Bundle and deploy on GitHub Pages

on:
  push:
    branches:
      - master

permissions:
  contents: write
  pages: write

jobs:
  # check:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4

  #     - name: Set up Node
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 20

  #     - name: Cache node modules
  #       uses: actions/cache@v3
  #       with:
  #         path: ~/.npm
  #         key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
  #         restore-keys: |
  #           ${{ runner.os }}-node-

  #     - name: Install dependencies
  #       run: npm install

  #     - name: Run eslint checks
  #       run: npm run lint

  #     - name: Run stylelint checks
  #       run: npm run lint:css
      
  #     - name: Run svelte-check
  #       run: npm run check
  build:
    runs-on: ubuntu-latest
    # needs: check
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Cache node modules
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: JSTraining/dist

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: JSTraining/dist

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: JSTraining
          publish_branch: gh-pages
          exclude_assets: '.github,**/.gitignore,.stylelintignore,.stylelintrc.json,eslint.config.js,package.json,package-lock.json,rollup.config.js,tsconfig.json'